#include <stdio.h>
#include "main.h"

/**
 * main - a function that prints the alphabet,
 * in lowercase, followed by a new line.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;

	for (n = 'a'; n <= 'z'; n++)
	{
	putchar(n);

	return (0);

	}

}
